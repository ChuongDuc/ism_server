query GetUserById($userId: Int!){
    getUserById(userId: $userId){
        id
        userName
        email
        fullName
        role
        address
        lastName
        firstName
        phoneNumber
        updatedAt
    }
}
